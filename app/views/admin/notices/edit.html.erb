<h1>Editing notice</h1>

<%= form_tag admin_notice_url(id: @notice.id) do %>
    <% if @notice.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@notice.errors.count, "error") %> prohibited this admin_notice from being saved:</h2>

          <ul>
            <% @notice.errors.full_notices.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="actions">
      <%= submit_tag %>
    </div>
<% end %>

<%= link_to 'Show', admin_notice_path(id: @notice.id) %> |
<%= link_to 'Back', admin_notices_path %>

<h1>Listing admin_notices</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @notices.each do |notice| %>
  <tr>
    <td><%= link_to 'Show', admin_notice_path(id: notice.id) %></td>
    <td><%= link_to 'Edit', edit_admin_notice_path(notice) %></td>
    <td><%= link_to 'Destroy', admin_notice_path(notice), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Message', new_admin_notice_path %>

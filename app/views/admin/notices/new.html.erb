<h1>New admin_notice</h1>

<%= form_tag admin_notices_path, method: 'post' do %>
    <% if @notice.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@notice.errors.count, "error") %> prohibited this admin_notice from being saved:</h2>

          <ul>
            <% @notice.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div>
      有効期限
      <%= datetime_select :notices, :closed_at %>
    </div>

    <div class="actions">
      <%= submit_tag %>
    </div>
<% end %>

<%= link_to 'Back', admin_notices_path %>
